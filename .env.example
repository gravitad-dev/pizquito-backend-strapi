HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified
# Upload provider (Cloudinary) base folder for scoped uploads
# CLOUDINARY_BASE_FOLDER=Strapi/pizquito
# --- Cron billing configuration ---
# Cron rule: if set, overrides default monthly billing schedule
# Example: runs at 03:00 on day 25 of each month
# BILLING_CRON_RULE=0 0 3 25 * *
# Note: Billing day is now configured via Strapi admin panel (Content Type: Cron Day)
